{{!-- TODO fix the mess with category vs tag naming --}}
{{!-- maybe we could just extend the component ? --}}
{{#paper-chips
  searchField="title"
  noMatchesMessage="Not found. Click to add."
  content=selectedCategories
  options=availableCategoryRecords
  removeItem=removeItem
  addItem=addItem
  placeholder=placeholder
  as |tag|
}}
  {{tag.title}}
{{/paper-chips}}

