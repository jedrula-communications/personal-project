
import { targetBlank } from 'jedrula-app/helpers/target-blank';
import { module, test } from 'qunit';

module('Unit | Helper | target blank');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = targetBlank([42]);
  assert.ok(result);
});

